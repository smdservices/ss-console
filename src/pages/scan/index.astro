---
/**
 * /scan retired (ADR 0002 Phase 1 PR-C, then doubly retired when Outside
 * View itself was killed). The middleware serves a 301 from /scan to /;
 * this page is the belt-and-suspenders fallback in case the middleware
 * redirect regresses.
 *
 * The verify path (/scan/verify/[token]) is intentionally NOT redirected
 * because magic-link tokens issued before retirement are still valid and
 * route to that landing page directly.
 *
 * This file is scheduled for deletion in a separate cleanup PR coordinated
 * with the conversation-mechanism team, after we've verified zero 404s on
 * /scan in production logs.
 */
return Astro.redirect('/', 301)
---
