---
/**
 * /scorecard retired (ADR 0002 Phase 1 PR-C, then doubly retired when
 * Outside View itself was killed). The middleware serves a 301 from
 * /scorecard to /; this page is the belt-and-suspenders fallback in
 * case the middleware redirect regresses.
 *
 * Pre-existing scorecard JS (src/scripts/scorecard.ts), the submit
 * endpoint (/api/scorecard/submit), and the scoring utilities under
 * src/lib/scorecard/ all stay on disk because removing them is
 * load-bearing regression risk. They're scheduled for the same cleanup
 * PR coordinated with the conversation-mechanism team.
 */
return Astro.redirect('/', 301)
---
