---
title: Claims 2026-08-25
---

# ss-console review — 2026-08-25

First run under `/code-review` v2. Run `bash docs/reviews/claims-2026-08-25.probe.sh`.

**3 closed · 7 still open · 0 regressed**

## Still open

Carried from 2026-08-23. These are `state: OPEN` — the probe holding means the
defect is still there, not that it was fixed. None has been acted on.

| # | Claim | Now |
|---|---|---|
| A6 | No security response headers anywhere in shipped code | 0 |
| F1 | `sticky_stop.py` (890 lines) has no code callers | 2 non-code refs |
| F3 | No test enumerates the broker's verb set | 0 |
| F4 | `trust_ceiling.py` absent from the parity manifest while claiming parity | 0 |
| F7 | `BrokerSuppressedWakeWriter` duplicated, no sync test | 4 copies |
| F9 | `sys.path.insert` stands in for packaging | 28 |
| F12 | Coverage thresholds declared, no workflow runs them | 0 |

## Closed this run

| # | Claim | Now |
|---|---|---|
| F15 | Manage-token redaction wired into both `scrubPath` returns | 3 |
| F17 | `EventsTracker` honours `disableAnalytics` | 1 |
| PC6 | Instrument liveness for A6 | 87 |

## New findings

None. The diff since `cc97518d` is this session's own work, already reviewed.

## Judgment

No commands, so no severity tags. Which of the seven above matters most depends
on the roadmap, which a review does not know. A6 and F4 are the two touching a
live client surface.

## Coverage

Two passes, both returned: diff (`cc97518d..HEAD`) and invariant (whole tree).
No agents dispatched — small enough to run directly, which is what v1's own text
said to do and its 2026-08-23 run did not.

**A6 now carries a positive control (PC6 = 87)**, which the 2026-08-23 finding
lacked. Same regex, same paths, same flags, on sibling header terms that are
present. Without it, "0 matches" is not evidence that the headers are absent —
only that the search returned nothing, which a broken search also does.

## Self-check, pasted verbatim

```
$ grep -cve '^[[:space:]]*$' docs/reviews/claims-2026-08-25.md   # max 60
50
$ grep -cE '^\| [A-F][+-]? ' docs/reviews/claims-2026-08-25.md   # must be 0
0
$ bash docs/reviews/claims-2026-08-25.probe.sh; echo "exit=$?"
3 closed · 7 still open · 0 regressed · 0 changed
exit=0
```

Probe proven able to fail: removing one `redactOpaqueSegments` call site reports
`REGRESSED F15` and exits 1. Restoring returns exit 0.
