"""Cost telemetry pricing references.

Hardcoded pricing JSON files for cost drivers whose source APIs do not
expose per-action prices. See `anthropic_pricing.json`. Loader helpers
live in `ai-employee/adapter/cost_ingest.py`.
"""
