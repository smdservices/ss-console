name: Tick ACs on PR merge

# Caller for the canonical reusable workflow hosted in crane-console.
# See https://github.com/venturecrane/crane-console/blob/main/docs/runbooks/ac-tick-workflow-rollout.md

on:
  pull_request:
    types: [closed]

jobs:
  tick:
    uses: venturecrane/crane-console/.github/workflows/tick-acs-on-merge-reusable.yml@6e2af4c88202f34843176538c95da9d3ec3946f7 # tick-acs-v1
    permissions:
      issues: write
      pull-requests: write
      contents: read
