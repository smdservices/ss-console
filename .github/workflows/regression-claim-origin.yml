name: Regression Claim-Origin

on:
  issues:
    types: [opened, labeled]

jobs:
  claim-origin:
    if: |
      contains(github.event.issue.labels.*.name, 'regression') ||
      github.event.label.name == 'regression'
    uses: venturecrane/crane-console/.github/workflows/regression-claim-origin-reusable.yml@a89c747370c3804a3e2e38d5410855b9f73e0147 # main
    secrets:
      CRANE_RELAY_KEY: ${{ secrets.CRANE_RELAY_KEY }}
