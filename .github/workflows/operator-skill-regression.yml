# Operator skill regression: issue #825.
#
# Runs every committed fixture through the regression harness and diffs the
# extracted output against the golden snapshot at
# `operator/tests/golden/<skill>/<fixture>.json`. A diff is a P0 merge
# block per PRD §17.4 ("cross-customer skill regression incidents: 0").
#
# NO external LLM or network calls. The reference `.md` file committed in
# `operator/skills/<skill>/fixtures/` is the ground truth. See
# `docs/specs/operator/skill-regression-ci.md` for the contract and
# `operator/tests/README-regression.md` for partner-facing bypass docs.
#
# Triggered on changes to skills, the adapter, or fixtures: the three
# surfaces whose drift could break in-flight customer skill pins.

name: operator-skill-regression

on:
  pull_request:
    paths:
      - 'operator/skills/**'
      - 'operator/adapter/**'
      - 'operator/fixtures/**'
      - 'operator/tests/skill_regression.py'
      - 'operator/tests/golden/**'
      # ADR 0022 Stream 4: PI skills + fixtures + golden tests live under
      # the vertical pack now.
      - 'operator/verticals/**'
      - '.github/workflows/operator-skill-regression.yml'
  push:
    branches: [main]

permissions:
  contents: read
  pull-requests: write

jobs:
  regression:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v5

      - name: Set up Python
        uses: actions/setup-python@v6
        with:
          python-version: '>=3.11'

      - name: Run skill regression
        id: regression
        run: |
          set +e
          python3 operator/tests/skill_regression.py \
            --markdown-out regression-report.md
          echo "exit_code=$?" >> "$GITHUB_OUTPUT"

      - name: Upload regression report
        if: always()
        uses: actions/upload-artifact@v6
        with:
          name: operator-skill-regression
          path: regression-report.md
          if-no-files-found: warn

      - name: Comment on PR
        if: github.event_name == 'pull_request' && always()
        uses: actions/github-script@v8
        with:
          script: |
            const fs = require('fs');
            const path = 'regression-report.md';
            if (!fs.existsSync(path)) {
              core.warning('regression-report.md not produced; skipping PR comment');
              return;
            }
            const body = fs.readFileSync(path, 'utf8');
            const marker = '<!-- operator-skill-regression -->';
            const fullBody = `${marker}\n${body}`;
            const { owner, repo } = context.repo;
            const issue_number = context.issue.number;
            const { data: comments } = await github.rest.issues.listComments({
              owner, repo, issue_number, per_page: 100,
            });
            const existing = comments.find(c => c.body && c.body.startsWith(marker));
            if (existing) {
              await github.rest.issues.updateComment({
                owner, repo, comment_id: existing.id, body: fullBody,
              });
            } else {
              await github.rest.issues.createComment({
                owner, repo, issue_number, body: fullBody,
              });
            }

      - name: Enforce regression gate
        if: steps.regression.outputs.exit_code != '0'
        run: |
          echo "skill regression failed (harness exit_code=${{ steps.regression.outputs.exit_code }})"
          echo "see the PR comment or the uploaded regression-report.md artifact for the per-fixture table"
          exit 1
